# Build output
*.zip
dist/
/docs/
/modules/
/DESIGN.md
/20??-??-??*.md
*.tar
*.bundle

# Local project metadata / release drafts
.code-abyss/
.ai_changelog.yaml
AUDIT.md
RELEASE_v*.md
readme.txt
release_review/

# Release assets are uploaded to GitHub Releases, not committed to source
modules/*/dist/
release-assets/

# Local evidence, clean-source snapshots and device/build scratch; never ship
# these private artifacts in the public module repository.
work/

# Runtime state belongs to the installed module, never the source tree
/.arbiter_*
/.bg_*
/.cpu_sched_owner
/.current_profile
/.feature_*
/.game_handoff_policy
/.idle_isolate_mode
/.install_receipt
/.locks/
/.logs/
/.nr_*
/.ntp_server
/.payload_state
/.power_*
/.profile_*
/.root_family
/.sched_owner_desired
/.scheduler_*
/.sim2_*
/.state_schema
/.standby_diag_state
/.swap_*
/.thermal_*
/.uecap_*
/.webui_*

# Nested repo copy (accidental, should not exist)
Pixel9Pro-Control/

# Device/TestLab fixture mirror; never source or release content
ADB/

# Python
__pycache__/
*.pyc
*.pyo

# Local Node/Playwright dependencies and reports
node_modules/
playwright-report/
test-results/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
