Pixel / UGT / fas-rs 调度 owner 与游戏 lease 分离，切换过程加锁并复读验证。
UGT → fas-rs 游戏接管前恢复 cpufreq 残留并确认 cap=1024，退出后恢复日常 cap=0。
当前策略切换增加进度说明和统一操作锁，避免重复点击与并发状态冲突。
减少 WebUI 重复调度器探测，实机状态读取约从 4.65 秒降至 3.68 秒。
温控档位改为客观阈值说明；UGT、fas-rs 与基带功能按实际安装状态显示。
统一应用名称与 UID 目录，功耗排行和后台限制使用一致的应用识别结果。
